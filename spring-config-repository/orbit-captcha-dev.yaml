profile: dev
server:
  port: 7006
spring:
  application:
    name: orbit-captcha
  data:
    redis:
      host: 172.17.0.1
      repositories:
        enabled: false
logging:
  level:
    root: info
    com.inmaytide.orbit: debug
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://orbit-discovery:7000/eureka/
