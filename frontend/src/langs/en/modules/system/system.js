import menu from './menu';
import func from './func';
import org from './org';
import user from './user';

export default {
  system: {
    menu: menu,
    func: func,
    org: org,
    user: user
  }
};
