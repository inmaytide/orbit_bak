{
  "instance": {
    "hostName": "${ipAddress}",
    "app": "orbit-attachment",
    "ipAddr": "${ipAddress}",
    "status": "UP",
    "port": "${port}",
    "securePort": "8443",
    "homePageUrl": "http://${ipAddress}:${port}/",
    "statusPageUrl": "http://${ipAddress}:${port}/info",
    "healthCheckUrl": "http://${ipAddress}:${port}/health",
    "dataCenterInfo": {
      "name": "orbit-attachment"
    },
    "metadata": {
      "instanceId": "orbit-attachment"
    }
  }
}