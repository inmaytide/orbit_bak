spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configurations
      profile: dev
      label: orbit-dev
eureka:
  client:
    service-url:
      defaultZone: http://discovery.orbit.com:7000/eureka/