spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configurations
      profile: dev
    # label: master
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka/